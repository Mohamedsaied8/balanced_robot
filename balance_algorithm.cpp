#include <iostream>

// Implement complementry filter..
template <class T>
void ComplementryFilter(T roll, T pitch, T yaw)
{

}

int main()
{
    double roll = 0.0, pitch = 0.0, yaw{0.0};
    ComplementryFilter<double>(roll, pitch, yaw);
    return 0;
}
